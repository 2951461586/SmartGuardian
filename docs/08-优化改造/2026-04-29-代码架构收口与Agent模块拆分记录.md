# 2026-04-29 代码架构收口与 Agent 模块拆分记录

## 目标

基于 2026-04-25 至 2026-04-29 全部优化改造文档，收口当前代码架构配置，更新项目文档，清理遗留问题，并对超大文件进行模块化拆分。

## 本批落地

### 1. CLAUDE.md 全面更新

- 从 Mock 时代描述更新为 AGC Serverless 主线。
- 补充 21 个云函数域、38 个 Cloud DB 集合、事件 outbox、通知编排、Storage 附件、安全审计、Agent 能力等最新架构。
- 更新常用命令：新增云函数冒烟、AGC 一致性、生产就绪、生产闭环、卡片一致性、Legacy 门禁等脚本。
- 更新结构要点：端侧服务层（agc/agent/distributed）、云函数域列表、脚本清单。
- 更新关键实现脉络：AGC 路由契约、事件驱动架构、卡片云端缓存、Push 端云协同、Agent 框架。
- 更新约束：AGC-only、生产验收变量、真机能力、LLM 配置。

### 2. AGENTS.md 同步更新

- 与 CLAUDE.md 保持一致，移除 Mock/BaseApiService/services/mock 等过时引用。
- 保留 ArkTS 语法约束和 HarmonyOS API 使用规范。

### 3. 生产代码 console.log 清理

- `pages/parent/ParentOrderDetailPage.ets:105`：移除活跃 `console.log('Cancel order')`，替换为 TODO 注释（需云函数 order 域补充 cancel 路由）。

### 4. Agent 云函数模块拆分

- 原 `agent/index.js`（941 行）拆分为三个模块：
  - `agent/navigation-routes.js`（148 行）：导航路由表，21 个角色-页面映射。
  - `agent/llm-client.js`（220 行）：LLM 配置、安全上下文构建、prompt 构建、API 调用。
  - `agent/index.js`（595 行）：路由处理器、上下文聚合、本地规则回答、Agent 响应构建。
- 拆分后总行数 963 行（原 941 行 + 新增 require 语句），模块职责清晰。

### 5. read-models.js 按域拆分（P1）

- 原 `shared/read-models.js`（1014 行）拆分为 9 个域子模块 + 1 个索引导出：
  - `shared/read-models/base.js`（106 行）：核心实体 fetcher（getUser, getStudent, getServiceProduct 等）。
  - `shared/read-models/session.js`（71 行）：getSessionView, getSessionStudents。
  - `shared/read-models/attendance.js`（209 行）：考勤视图与报表。
  - `shared/read-models/homework.js`（53 行）：作业任务与反馈视图。
  - `shared/read-models/message.js`（101 行）：消息详情与统计。
  - `shared/read-models/alert.js`（44 行）：告警统计。
  - `shared/read-models/refund.js`（42 行）：退款视图与统计。
  - `shared/read-models/card.js`（160 行）：今日状态卡与异常告警卡。
  - `shared/read-models/report.js`（268 行）：财务报表、教师绩效、工作台。
  - `shared/read-models/index.js`（21 行）：聚合导出。
- 原 `read-models.js` 保留为 7 行 re-export，向后兼容所有现有消费者。

### 6. 端侧大文件治理（P1→P2）

#### FormCardSyncService.ets（549 → 153 行）

拆分为 4 个文件：
- `FormCardDataModels.ets`（64 行）：数据模型类（FormCardStudentData, FormCardAlertData, FormCardSyncResult, FormCardBindingPayload）。
- `FormCardStorage.ets`（218 行）：Preferences 存储层、数据合并、fallback 数据。
- `FormCardDataMapper.ets`（199 行）：API 模型 → 表单载荷映射、格式化工具。
- `FormCardSyncService.ets`（153 行）：薄编排层，委托 Storage 和 Mapper。

#### ApiWrapper.ts（528 → 464 行）

- 提取 `ApiTypes.ts`（71 行）：HttpMethod 枚举、RequestOptions/ApiResponse/CacheEntry 接口。
- ApiWrapper.ts 从 ApiTypes.ts 导入类型，index.ts 同步更新。

#### Card.ets（523 行，未拆分）

已包含 4 个独立 @Component（Card, SimpleCard, StatCard, InfoCard, ActionCard），每个均 <100 行，结构内聚，无需进一步拆分。

### 7. 优化改造索引补全

- `docs/08-优化改造/README.md` 执行记录新增本文档索引。

## 验证记录

- `npm run smoke`（cloud-functions）：通过，覆盖 21 个函数域。
- `.\scripts\check-agc-consistency.ps1`：通过，AGC contracts 21 / manifest functions 21 / Cloud DB collections 38。

## 后续改造清单

| 优先级 | 改造项 | 当前状态 | 建议落点 |
|---|---|---|---|
| P0 | 生产环境变量配置 | 本机未配置 Push/Storage/trigger 凭据 | AGC 控制台环境变量 |
| P0 | Push Kit 真 token 接入 | 端侧 provider 已就绪，缺真实 SDK adapter | `PushTokenProviderService` + Push Kit SDK |
| P0 | 事件异步消费触发 | 手动补偿入口已就绪，缺 AGC 定时触发器 | AGC 触发器 → `/api/v1/events/trigger` |
| P1 | Cloud Storage 真实上传 | 端侧编排已就绪，缺 AGC SDK adapter | `CloudStorageUploadService` + Cloud Storage SDK |
| P1 | 卡片真机一致性验收 | 静态/云端检查通过，缺真机 hdc 连接 | `check-form-card-consistency.ps1 -RequireDevice` |
| P1 | 分布式能力替换 | 当前为本地兼容模式 | `services/distributed/` → 真实软总线 API |
| P1 | ~~read-models.js 拆分~~ | ✅ 已拆分为 9 个域子模块（每个 <270 行） | `shared/read-models/` |
| P1 | ~~端侧大文件治理~~ | ✅ FormCardSyncService(153)、ApiWrapper(464) | 提取子模块完成 |
| P2 | 运营报表预聚合 | 有读模型，缺定时聚合任务 | `report` 域新增聚合 job |
| P2 | 远程配置云端化 | 仍以代码/env 为主 | AGC Remote Config 或 `remote_configs` 集合 |
| P2 | Agent 策略与知识库 | 策略偏代码内置 | 新增策略/提示词模板/知识库集合 |
| P2 | 审计治理后台 | 审计 API 已落，缺管理端页面 | `admin/AdminAuditPage` |
| P2 | 支付/退款对账 | 基础函数已落，缺对账任务 | 新增对账任务、幂等表、回调验签 |
| P2 | 数据导出异步化 | 具备报表和 Storage 基础 | 新增 `export_tasks` |
| P2 | 多机构配置 | 基础组织已入库，机构级规则未收口 | 机构级服务规则、价格策略 |
