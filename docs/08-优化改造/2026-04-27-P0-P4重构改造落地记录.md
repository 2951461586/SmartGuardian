# 2026-04-27 P0-P4 重构改造落地记录

## 本轮范围

- P0：请求中台、运行环境与 ArkTS 语法约束收口。
- P1：AGC 契约、OpenAPI、Cloud Functions 一致性校验补齐。
- P2：HarmonyOS 官方能力接入边界收口。
- P3：三端页面与通用组件 UI token / LazyForEach 治理。
- P4：文档索引、执行记录与门禁顺序收口。

## 代码落点

- 请求与 API：`utils/request.ts`、`utils/api/ApiWrapper.ts`、`utils/api/ApiResult.ts`、`services/api/BaseApiService.ets`。
- AGC 契约：`services/agc/AgcFunctionContracts.ts`、`openapi-full.yaml`、`check-agc-consistency.ps1`、`cloud-functions/README.md`。
- 官方能力边界：`HarmonyAccountService.ets`、`HarmonyPushEndpointService.ets`、`HarmonyCapabilityService.ets`、`NotificationEndpointRegistrationService.ets`。
- 页面治理：`ParentSettingsPage.ets`、`ParentChildrenPage.ets`、`ParentServicesPage.ets`、`ParentAboutPage.ets`、`ParentProfileEditPage.ets`、`TeacherSchedulePage.ets`。
- 组件治理：`WorkbenchActionGrid.ets`、`ActionButton.ets`、`ReportControlDeck.ets`、`StudentProfileSections.ets`、`AdminOrderSections.ets`。
- 文档与门禁：`docs/08-优化改造/README.md`、`2026-04-27-重构改造大颗粒执行清单.md`、`run-refactor-gates.ps1`。

## 验证命令

- `node scripts\cloud-functions-smoke.js`
- `powershell -ExecutionPolicy Bypass -File scripts\check-ui-governance.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts\check-agc-consistency.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts\run-refactor-gates.ps1`
- `.\hvigorw.bat assembleHap --analyze=advanced`

## 验证结果

- 页面注册：47 个已注册页面与 47 个 `@Entry` 页面一致。
- AGC 一致性：16 个函数域、18 个契约前缀、16 个 manifest 函数、32 个 Cloud DB collection 通过检查。
- UI 治理：页面与通用组件硬编码视觉 token 为 0。
- 应用代码构建 warning：0。
- Cloud Functions smoke：16 个业务域通过。
- HAP 构建：`assembleHap --analyze=advanced` 通过。

## 遗留项

- AGConnect SDK 依赖包仍有外部 warning，来源位于 `oh_modules/.ohpm/@hw-agconnect...`，不属于应用代码。
- 本地缺少 `signingConfig`，当前仅保留签名提示，未在仓库写入个人签名配置。
- `TeacherHomeworkPage.ets` 与 `ParentHomePage.ets` 超过页面体量 warning 线但未超过 fail 线，后续可拆为局部组件。
