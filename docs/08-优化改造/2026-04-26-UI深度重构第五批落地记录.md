# SmartGuardian UI 深度重构第五批落地记录
日期：2026-04-26

## 本轮目标

- 继续处理 `TeacherHomePage` 的首页分段与响应式拆分
- 清理 `ParentServiceDetailPage`、`ParentOrderDetailPage` 的页面层视觉字面量
- 将 `AdminAlertsPage` 与 `AdminRefundsPage` 迁入统一工作台模板与状态协议

## 本轮落地

### 1. 教师首页切入统一 Dashboard 模板

- 页面：
  - `entry/src/main/ets/pages/teacher/TeacherHomePage.ets`
- 新增教师首页区块组件：
  - `entry/src/main/ets/components/common/TeacherHomeSections.ets`
- 本轮收口内容：
  - 页面切到 `DashboardPageTemplate`
  - 首页拆成 `hero / workbench / session` 三段
  - 继续使用 `GreetingHeader`、`TeacherSessionHeroCard`、`TeacherSessionStatsCard`、`TeacherSessionsPreviewCard`
  - 分栏布局统一交给 `ResponsiveSplitPanel`
- 结果：
  - `TeacherHomePage` 页面体量压到 `432` 行
  - 教师首页退出页面体量告警线

### 2. 家长详情页继续回收视觉字面量

- 页面：
  - `entry/src/main/ets/pages/parent/ParentServiceDetailPage.ets`
  - `entry/src/main/ets/pages/parent/ParentOrderDetailPage.ets`
- 本轮收口内容：
  - 价格、状态、按钮、阴影、底栏背景改走 `HarmonyColor / HarmonyReport / harmonyShadowColor`
  - 白底、灰底、分割线和风险色不再由页面层直接写死
- 结果：
  - 两个详情页脱离页面层高噪声名单

### 3. 管理端预警/退款页切入统一工作台模板

- 页面：
  - `entry/src/main/ets/pages/admin/AdminAlertsPage.ets`
  - `entry/src/main/ets/pages/admin/AdminRefundsPage.ets`
- 新增管理端运营区块组件：
  - `entry/src/main/ets/components/common/AdminOperationSections.ets`
- 本轮收口内容：
  - 两页统一切到 `WorkbenchListPageTemplate`
  - 统一使用 `AdminOverviewMetricSection + AdminStatusFilterCard + StatefulContainer`
  - 告警列表与退款列表都沉淀为公共区块
- 结果：
  - 管理端预警/退款页与报表页进入同一套页面编排和状态协议

## 验证结果

已执行：

- `cmd /c hvigorw.bat assembleHap --analyze=advanced`
- `powershell -ExecutionPolicy Bypass -File scripts/check-page-budget.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/check-ui-governance.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/run-refactor-gates.ps1`

结果：

- `hvigor` 构建通过
- 页面体量门禁通过
- UI 治理门禁通过
- 全量重构门禁通过

## 指标变化

- 页面层硬编码视觉 token：`132`
- `components/common` 硬编码视觉 token：`146`
- 页面体量超 450 行：`0`

## 当前剩余重点

- 页面层高噪声文件继续前移到：
  - `AdminHomePage.ets`
  - `ParentPaymentPage.ets`
  - `AdminSessionsPage.ets`
  - `TeacherAttendancePage.ets`
  - `TeacherStudentDetailPage.ets`
- 通用组件层高噪声文件仍集中在：
  - `AdminSummaryHeroCard.ets`
  - `TeacherSessionHeroCard.ets`
  - `ParentStudentHeroCard.ets`
  - `ParentOrderCreateSections.ets`
  - `MessageListItem.ets`

## 下一批建议

- 继续拆 `AdminHomePage`，把首页级运营卡片和动态流沉淀为独立 section
- 收 `ParentPaymentPage`、`TeacherAttendancePage` 的页面层视觉字面量
- 继续清理 `AdminSummaryHeroCard`、`TeacherSessionHeroCard`、`ParentStudentHeroCard` 三个 Hero 组件的残余硬编码 token
