# SmartGuardian 全栈深度扫描与重构清单

> 日期：2026-04-25  
> 范围：HarmonyOS ArkTS 前端 + AGC Serverless / Cloud Functions / Cloud DB 后端 + `docs/` 文档体系

## 已完成扫描

- ✔ 已扫描 `docs/01-*`、`docs/02-*`、`docs/03-*`、`docs/08-*` 现有设计、接口、架构和配置文档
- ✔ 已扫描前端页面入口、工作台壳、状态层、请求层、Mock 层和页面注册
- ✔ 已扫描既有后端 Controller / Service / SQL / 返回体风格，并确认不再沿用为主架构
- ✔ 已确认后端目标路线切换为 `AGC Serverless + Cloud Functions + Cloud DB`
- ✔ 已结合华为官方文档确认重构重点落在工作台协议、请求中台、云函数分域、Cloud DB、官方 Kit 接入和测试门禁

## P0 骨架重构

1. ✔ 统一三端工作台壳：`MainPage.ets` 已改成 manifest 驱动，导航树改由 `WorkbenchRegistry` 统一管理
2. ✔ 页面注册收口：`entry/src/main/resources/base/profile/main_pages.json` 已补齐，页面注册校验脚本已建立
3. ✔ 全局状态收口：登录态、环境态、导航态、manifest 缓存键已统一
4. ✔ 请求中台收口：`request.ts` 和 `ApiWrapper.ts` 已统一鉴权、401、错误码和跳转逻辑
5. ✔ 工作台 manifest 同源：前端 shell、`WorkbenchService`、Mock 和接口文档已对齐
6. ✔ AGC 请求适配层已建立：前端保留 `/api/v1/*` 路由，底层已切到 AGC 网关分发
7. ✔ `services/api/*` 已补齐 AGC 域元数据和云函数契约索引
8. ✔ `openapi-full.yaml` 已重建为 AGC 契约视角
9. ✔ `cloud-functions/` 实际目录骨架已建立
10. ✔ Cloud DB 模型迁移清单已同步到代码骨架和文档

## P1 业务重构

11. ✔ 页面大拆分：`LoginPage.ets`、三端首页、报表页、订单创建页已完成组件化收口，并补充页面体量预算脚本
12. ☐ 大列表虚拟化：消息、考勤、订单、报表、班次、退款等页面统一切到 `List` / `LazyForEach`
13. ✔ Mock 分域拆分：`mockService.ts` 已拆成按域 handler，`mockData.ts` 已收口为 `data/*` barrel 出口
14. ✔ 云函数业务实现：16 个函数域已补齐真实 handler、鉴权、参数校验、聚合读模型和本地数据访问层
15. ✔ Cloud DB 实体落库：学生、家长、机构、订单、班次、考勤、作业、消息、退款、支付、时间线、工作台等集合已完成字段级建模
16. ✔ 接口契约闭环：OpenAPI、云函数契约、前端服务层、Cloud DB 模型与请求适配层已收口到 AGC 口径

## P2 能力升级

17. ☐ 官方能力接入：`Account Kit`、`Push Kit`、`Location Kit`、分析与监控能力
18. ✔ 测试门禁补齐：ArkTS 单测已补 AGC 契约/偏好更新校验，页面注册、AGC 一致性与页面体量预算已收口到统一脚本门禁
19. ✔ 体量治理：主业务列表继续切到 `LazyForEach`，并新增页面体量预算检查，防止超大页面回流
20. ✔ 文档一致性机制：已建立页面注册校验和 `check-agc-consistency.ps1`，自动对齐 manifest、OpenAPI、Cloud DB、Mock handler

## 当前执行顺序

1. 先收完 P0，确保协议、骨架、文档、目录、前端调用链全部切到 AGC 口径
2. 再推进 P1，开始写真实云函数、Cloud DB 模型和业务 handler
3. 最后补 P2，把官方 Kit 和测试门禁补齐

## 今日新增落地

- ✔ `cloud-functions/shared/` 已补齐 `router.js` `auth.js` `store.js` `schema.js` `read-models.js` `errors.js` `time.js`
- ✔ `cloud-functions/cloud-db/schema.json` 与 `seed-data.json` 已作为 Cloud DB 建模与本地联调底座
- ✔ `entry/src/main/ets/pages/parent/ParentServicesPage.ets` 已移除旧数组兜底，直接对齐 AGC 服务产品数组契约
- ✔ 前端已消除服务产品、时间线、班次等数组型接口的旧分页/数组双形态兼容分支
- ✔ `entry/src/main/ets/services/mock/mockService.ts` 已拆分为按域 handler，Mock 路由入口从 1381 行单体文件收口为统一分发层
- ✔ `scripts/check-agc-consistency.ps1` 已接入页面注册、AGC 契约、云函数清单、Cloud DB 集合和 Mock handler 一致性校验

## 官方参考

- [HarmonyOS 开发文档入口](https://developer.huawei.com/consumer/cn/doc/)
- [AGC Serverless](https://developer.huawei.com/consumer/cn/agconnect/serverless/)
- [AGC Cloud Functions](https://developer.huawei.com/consumer/cn/agconnect/cloud-function/)
- [AGC Cloud DB](https://developer.huawei.com/consumer/cn/agconnect/cloud-base/)
## 2026-04-25 update
- [x] `mockData.ts` has been downshifted into `data/*`, with `mockData.ts` kept as a barrel export only.
- [x] Three-role home pages and reports/order-create pages have started component extraction.
- [x] Messages, orders, attendance, and refunds have been moved to `List` rendering.
- [x] `AdminOrdersPage.ets`, `TeacherMessagesPage.ets`, `TeacherHomeworkPage.ets`, `ParentServicesPage.ets`, and `AdminServicesPage.ets` have been aligned to single-scroll `List` structures.
- [x] `ServiceProductCard.ets` was rebuilt as a null-safe shared card so parent/admin service pages now share one contract-safe renderer.
- [x] `ParentHomePage.ets`, `AdminReportsPage.ets`, and `ParentOrderCreatePage.ets` have been split into reusable section components and remain build-pass after refactor.
- [x] `TeacherHomePage.ets`, `AdminHomePage.ets`, and `ParentTimelinePage.ets` have been further decomposed into reusable dashboard/timeline sections, with `ParentTimelinePage.ets` aligned to `SelectionSheet + List` based rendering.
- [x] `AdminStudentsPage.ets`, `AdminSessionsPage.ets`, and `TeacherSchedulePage.ets` now use a shared `LazyArrayDataSource` and have been aligned to single-scroll `List + LazyForEach` rendering for their primary data flows.
- [x] `ParentLeavePage.ets`, `ParentRefundPage.ets`, and `AdminStudentDetailPage.ets` have been refactored into section-oriented page lists, with refund/history and student-detail dynamic blocks moved onto `LazyForEach`-backed rendering.
- [x] `ParentHomeworkPage.ets`, `ParentProfilePage.ets`, and `AdminMessagesPage.ets` were rebuilt around `List`, shared section cards, and `LazyArrayDataSource`-driven primary flows.
- [x] `LoginPage.ets`, `TeacherProfilePage.ets`, and `AdminProfilePage.ets` have now been restructured into card-based sections, with shared profile/login building blocks extracted to `components/common/`.
- [x] P1 closure: primary high-volume business lists have been moved onto `List` / `LazyForEach`, and the remaining page-level `ForEach` usage is limited to low-volume internal section rendering rather than main data flows.
- [x] `ParentMessagesPage.ets`, `TeacherMessagesPage.ets`, `ParentOrdersPage.ets`, and `TeacherAttendancePage.ets` now use shared `LazyArrayDataSource` backed primary lists, completing the remaining high-volume page closures.
- [x] NotificationKit based badge sync and local attendance/message reminders have been centralized through `HarmonyCapabilityService.ets`, with admin message dispatch, parent settings, and teacher/common scan flows wired in.
- [x] `run-refactor-gates.ps1` and `check-page-budget.ps1` now complement the existing registry and AGC consistency checks for P2 gatekeeping.
- [x] Cloud DB real seed import now runs against AGC with `agc-apiclient-smartguardian.json` and `ClouddbDev`, and `node scripts/apply-cloud-db-seed.js --reset` completes with 71 seeded rows.

## 2026-04-26 Closure Update
- [x] P1 real AGC backend path is closed: Cloud DB object types imported, seed reset completed, cloud-function smoke passed, AGC consistency check passed, and `hvigorw assembleHap --analyze=advanced` remains green.
- [x] P1 frontend request path is now verifiable end-to-end against AGC Serverless instead of mock-only validation.
- [x] P2 LocationKit has been wired into QR attendance flows. `QRCodeScanPage.ets` and `TeacherScanPage.ets` now request runtime location permission, capture current coordinates through `@kit.LocationKit`, reverse-geocode when available, and submit the resolved location string with attendance sign-in/sign-out.
- [x] P2 notification baseline remains on HarmonyOS `NotificationKit` with badge sync and local reminder preview.
- [ ] P2 Huawei Account service and Push Kit are still not true SDK integrations in the current repo. The installed client packages only cover AGConnect core/auth/credential, and no dedicated Harmony Account/Push client package is present yet.
## 2026-04-26 P1+P2 收口补记

- 已补齐通知点击回调链：本地通知现在会携带 `smartguardian.route` 和 `smartguardian.params`，冷启动、登录后跳转、热启动通知点击都已接入 [NotificationUtil.ets](/F:/HarmonyOSProjects/SmartGuardian/entry/src/main/ets/utils/NotificationUtil.ets:1)、[NotificationLaunchService.ets](/F:/HarmonyOSProjects/SmartGuardian/entry/src/main/ets/services/NotificationLaunchService.ets:1)、[EntryAbility.ets](/F:/HarmonyOSProjects/SmartGuardian/entry/src/main/ets/entryability/EntryAbility.ets:1)、[Index.ets](/F:/HarmonyOSProjects/SmartGuardian/entry/src/main/ets/pages/Index.ets:1)、[LoginPage.ets](/F:/HarmonyOSProjects/SmartGuardian/entry/src/main/ets/pages/LoginPage.ets:1)。
- 已补齐登录后的通知端点注册链：前端会在 AGC 模式登录成功后和应用启动初始化后尝试绑定当前会话的通知端点元数据，服务入口为 [NotificationEndpointRegistrationService.ets](/F:/HarmonyOSProjects/SmartGuardian/entry/src/main/ets/services/NotificationEndpointRegistrationService.ets:1)；云函数入口为 [auth/index.js](/F:/HarmonyOSProjects/SmartGuardian/cloud-functions/auth/index.js:1) 的 `POST /api/v1/auth/session-device`。
- 已完成会话注册字段设计并写回 Cloud DB schema：[schema.json](/F:/HarmonyOSProjects/SmartGuardian/cloud-functions/cloud-db/schema.json:1) 中 `user_sessions` 新增了 `deviceId`、`notificationToken`、`notificationProvider`、`authUid`、`authPhone`、`clientVersion`、`clientPlatform`、`lastActiveAt`。
- AGC 真实 Zone `ClouddbDev` 的 `user_sessions` 对象类型已手动补齐新增字段后，`session-device` 现已切回真实持久化路径。
- 已将本地 Cloud DB schema 与 `ClouddbDev` 的 `user_sessions` 结构重新对齐，并重生成产物用于后续控制台同步：[migration-plan.generated.json](/F:/HarmonyOSProjects/SmartGuardian/cloud-functions/cloud-db/migration-plan.generated.json:1)、[agc-object-types.generated.json](/F:/HarmonyOSProjects/SmartGuardian/cloud-functions/cloud-db/agc-object-types.generated.json:1)、[agc-object-types.importable.generated.json](/F:/HarmonyOSProjects/SmartGuardian/cloud-functions/cloud-db/agc-object-types.importable.generated.json:1)。
