# 2026-04-26 文档归档与应用框架重构执行清单

> 范围：`docs/08-优化改造/` 文档治理、`entry/` HarmonyOS Stage 应用框架、`cloud-functions/` AGC Serverless 共享框架  
> 基线：以 HarmonyOS 应用框架、ArkUI、AGC Cloud Functions / Cloud DB 官方文档为准  
> 状态：P0 / P1 / P2 已收口，P3 作为后续扩展项保留

## 参考基线

- [HarmonyOS 开发文档入口](https://developer.huawei.com/consumer/cn/doc/)
- [ArkUI 指南](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkui)
- [Cloud DB 新增对象类型](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/cloudfoundation-database-add-object)
- [Cloud DB 部署数据库](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/agc-harmonyos-clouddev-deploydatabase)
- [AGC Cloud Functions](https://developer.huawei.com/consumer/cn/agconnect/cloud-function/)

## 当前结论

- `docs/08-优化改造/` 已改为“主线总览 / 专项方案 / 执行记录”的索引结构。
- 前端启动链路已经从页面内分散判断，收口为 `AppLaunchCoordinatorService + SessionBootstrapService + RoleWorkbenchHost`。
- `MainPage` 当前只保留会话启动、manifest 加载、导航状态和底部导航，不再直接装配三端页面。
- 云函数共享路由层已经支持必填字段、字段规则和统一响应包装，业务 handler 的参数校验与返回体模式开始收口到框架层。

## P0 文档归档与治理

- [x] 重写 `docs/08-优化改造/README.md`，明确主线方案、专项设计、执行记录三层结构。
- [x] 新增本清单，作为“文档治理 + 应用框架重构”的总入口。
- [x] 固化新增文档规则：同类内容优先写入主线方案或批次记录，不再继续新增平级重复说明。

## P1 前端应用框架收口

- [x] 抽离 `AppLaunchCoordinatorService`，统一入口页分流、通知跳转目标解析、失效会话锁定。
- [x] 抽离 `SessionBootstrapService`，统一 `MainPage` 的本地会话判断、`auth/me` 校验和角色恢复。
- [x] 把 `AppInitializationService` 的总状态判断改成显式任务清单视角，区分 AGC 必需条件与通用初始化项。
- [x] 新增 `RoleWorkbenchHost`，把 `MainPage` 中的三端页面装配下沉为角色工作台宿主组件，缩小主壳页面职责。

## P2 后端共享框架收口

- [x] 把云函数必填参数校验下沉到 `cloud-functions/shared/router.js`，业务 handler 只保留领域逻辑。
- [x] 为共享路由层补齐 `queryRules` / `bodyRules` 规则描述能力，支持必填和基础类型校验。
- [x] 补齐 `detail` / `command` 响应包装，先在 `auth` 域接入登录、刷新、登出和 `session-device` 链路。

## P3 后续扩展项

- [ ] 把启动初始化结果暴露为可观测状态，支持首页或调试页展示关键能力 readiness。
- [ ] 增补 Cloud Functions 共享框架的路由级单测，覆盖 401 / 403 / 404 / 必填字段 / 类型校验。
- [ ] 补一份“Stage 应用壳 + AGC Serverless 后端壳”的架构图文档，作为后续新成员入口。

## 本轮代码落点

- `entry/src/main/ets/services/AppLaunchCoordinatorService.ets`
- `entry/src/main/ets/services/SessionBootstrapService.ets`
- `entry/src/main/ets/components/common/RoleWorkbenchHost.ets`
- `entry/src/main/ets/pages/Index.ets`
- `entry/src/main/ets/pages/MainPage.ets`
- `entry/src/main/ets/services/AppInitializationService.ets`
- `cloud-functions/shared/router.js`
- `cloud-functions/auth/index.js`

## 验收口径

- `Index.ets` 不再直接感知 token、通知目标、登录态三套分支。
- `MainPage.ets` 不再直接导入三端业务页面，也不再自己承担 `auth/me` 校验和失效会话清理。
- 云函数域 handler 可以通过 `bodyRules` / `queryRules` 声明参数要求。
- `run-refactor-gates.ps1`、`cloud-functions-smoke.js`、`assembleHap --analyze=advanced` 均通过后，本清单的 `P0/P1/P2` 视为完成。
