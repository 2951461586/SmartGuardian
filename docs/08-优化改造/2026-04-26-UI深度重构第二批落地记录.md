# SmartGuardian UI 深度重构第二批落地记录

日期：2026-04-26

## 本轮目标

- 将 `Profile + Schedule + Refund` 三组高噪声页面继续收口到统一模板体系。
- 继续压降页面层硬编码视觉 token。
- 保持 `hvigor` 构建和现有重构门禁稳定通过。

## 本轮落地

### 1. Profile 模板化

- 新增统一资料页模板：
  - `entry/src/main/ets/components/common/SceneTemplates.ets`
  - `ProfileWorkspaceTemplate`
- 新增统一资料页段组件：
  - `entry/src/main/ets/components/common/ProfileCenterSections.ets`
  - `ProfileHeroCard`
  - `ProfileUtilityButton`
  - `ProfileHighlightBanner`
- 三端资料页已切换：
  - `entry/src/main/ets/pages/parent/ParentProfilePage.ets`
  - `entry/src/main/ets/pages/teacher/TeacherProfilePage.ets`
  - `entry/src/main/ets/pages/admin/AdminProfilePage.ets`

### 2. Schedule 模板化

- `entry/src/main/ets/pages/teacher/TeacherSchedulePage.ets`
  - 切换到 `WorkbenchListPageTemplate`
  - 接入 `StatefulContainer`
  - 周视图和课表卡片统一回收到 `Card + StatCardRow + SessionBadge`

### 3. Refund 模板化

- `entry/src/main/ets/pages/parent/ParentRefundPage.ets`
  - 切换到 `PageScaffold`
  - 接入固定 footer 提交协议 `FormActionBar`
  - 历史记录改成 `StatefulContainer`
  - 订单概览、退款测算、退款表单统一回收到卡片体系

## 门禁结果

已执行：

- `cmd /c hvigorw.bat assembleHap --analyze=advanced`
- `powershell -ExecutionPolicy Bypass -File scripts/check-page-budget.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/check-ui-governance.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/run-refactor-gates.ps1`

结果：

- 构建通过
- 全部重构门禁通过
- 页面层硬编码视觉 token 从上一批的 `409` 继续下降到 `279`

## 当前剩余重点

- 页面体量仍超过 450 行的页面：
  - `AdminReportsPage.ets`
  - `ParentHomePage.ets`
  - `ParentRefundPage.ets`
  - `TeacherHomePage.ets`
- 下一批优先继续拆：
  - `AdminReportsPage`
  - `ParentHomePage`
  - `ParentRefundPage`
  - `AdminStudentSections`
  - `AdminOrderSections`
