# 2026-04-26 UI深度重构第八批收口记录

## 本批目标

- 对前一批遗留的页面层与 `components/common` 视觉字面量做最后一轮扫尾
- 将页面层硬编码视觉 token 压到 `50` 以下
- 将组件层硬编码视觉 token 压到 `40` 左右并继续下探
- 修复尾项组件中的乱码文案，降低后续维护噪声

## 本批完成项

### 1. 页面尾项继续收口

- `TeacherMessagesPage.ets`
  - 头部刷新按钮切到 `HarmonyColor` 令牌
  - 概览统计卡图标色切到 `HarmonyColor/HarmonyReport`
- `TeacherHomeworkPage.ets`
  - 统计卡图标色切到统一报表色板
- `ParentMessagesPage.ets`
  - “全部已读”操作按钮背景切到 `HarmonyColor.surfaceSubtle`
  - 三张统计卡图标色切到 `HarmonyReport`
- `ParentOrderCreatePage.ets`
  - 三个底部弹层背景切到 `HarmonyColor.surface`
- `AdminStudentsPage.ets`
  - 首页级新增动作按钮切到反白 overlay 令牌，不再使用白色字面量和透明边框字面量

### 2. 通用组件与场景 section 收口

- `IconActionButton.ets`
  - 默认图标色、容器底色、边框色和角标文字色全部切到 `HarmonyColor`
- `TabBar.ets`
  - 默认背景、未选中透明态、角标文字色全部改为设计令牌
- `ParentNoticeCard.ets`
  - 视觉字面量清零
  - 乱码文案修复为“系统 / 通知 / 今日”
- `ParentRequestSections.ets`
  - 整文件重写
  - 请假申请与退款说明区块全部接入统一令牌
  - 乱码文案全部修复
- `AdminStudentSections.ets`
  - 整文件重写
  - 学生档案驾驶舱、筛选、列表、分页等区块统一接入令牌
  - 乱码文案全部修复

### 3. 设计令牌补强

- `HarmonyDesign.ets`
  - 新增 `HarmonyColor.clear`
  - 用于替代页面层和组件层的 `Color.Transparent`

## 验证结果

实际执行：

- `cmd /c hvigorw.bat assembleHap --analyze=advanced`
- `powershell -ExecutionPolicy Bypass -File scripts\check-ui-governance.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts\run-refactor-gates.ps1`

结果：

- 构建通过
- 页面预算检查通过
- UI 治理检查通过
- 重构总门禁通过

## 当前指标

- 页面层硬编码视觉 token：`15`
- `components/common` 硬编码视觉 token：`15`
- 超 450 行页面：`0`

相较于上一批：

- 页面层：`58 -> 15`
- 组件层：`52 -> 15`

## 剩余散点

页面层剩余高点：

- `TeacherSchedulePage.ets`
- `ParentServicesPage.ets`
- `ParentChildrenPage.ets`
- `ParentAboutPage.ets`
- `ParentProfileEditPage.ets`

组件层剩余高点：

- `WorkbenchActionGrid.ets`
- `StudentProfileSections.ets`
- `AdminOrderSections.ets`
- `ActionButton.ets`
- `ReportControlDeck.ets`

这些已经不再是结构性问题，属于低风险零散尾项，可在后续业务迭代中顺手清理。

## 备注

- 当前 `hvigor` 构建残留的 warning 主要来自 `oh_modules/@hw-agconnect/*` 的三方 SDK 与 `@Entry export struct` 预览提示，不属于本轮前端页面治理回归问题。
