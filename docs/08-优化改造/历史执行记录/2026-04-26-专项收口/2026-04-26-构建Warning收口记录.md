# 2026-04-26 构建 Warning 收口记录

> 范围：AGConnect 三方包 warning、ArkUI `@Entry` 预览 warning、签名配置 warning  
> 验证命令：`hvigorw.bat assembleHap`、`scripts/check-build-warnings.ps1`

## 已收口

- [x] 页面级 `@Entry export struct` 预览提示已清理。路由注册页保留非导出 wrapper，复用组件改为 `*Content` 导出。
- [x] 卡片页 `@Entry` 参数提示已清理。`TodayGuardianCard`、`AbnormalAlertCard` 已补 `LocalStorage` 入参。
- [x] `StatusBadge` 的可选 `@Prop` warning 已清理。自定义文本、前景色、背景色改为默认空字符串。
- [x] 新增 `scripts/check-build-warnings.ps1`，用于把构建 warning 分成应用源码、AGConnect SDK、签名配置三类，门禁应用源码 warning 必须为 0。

## 当前剩余

- AGConnect SDK warning 仍来自 `oh_modules/.ohpm/@hw-agconnect+base-ohos@1.1.2` 与 `@hw-agconnect+crypto-ohos@1.1.2`，包括异常处理、废弃 API、`libagccrypto.so` 校验和 `page_show` 资源冲突。这些 warning 属于三方包源码，项目侧不直接修改 `oh_modules`。
- 签名 warning 仍为 `No signingConfig found for product default`。现有 `.ohos/signing` 材料存在，但没有与当前材料匹配的 DevEco/Hvigor 加密口令；历史加密口令验证失败，不能伪造 signingConfig。

## 后续处理规则

- AGConnect SDK warning：等待华为 AGConnect HarmonyOS SDK 发布兼容当前 ArkTS 编译器的包后升级；升级前由 `check-build-warnings.ps1` 分类记录，不计入应用源码 warning。
- 签名 warning：在 DevEco Studio 的 `Project Structure > Signing Configs` 重新导入/生成签名材料，让工具写入 `build-profile.json5` 的加密 `storePassword` 与 `keyPassword` 后再提交配置。
- 应用源码 warning：不得新增。运行 `powershell -ExecutionPolicy Bypass -File scripts/check-build-warnings.ps1 -RunBuild` 验证。

## 本轮验证结果

- `hvigorw.bat assembleHap`：构建通过。
- `scripts/check-build-warnings.ps1 -LogPath build-warning-normal.log`：应用源码 warning 为 0，AGConnect SDK warning 为 49，签名 warning 为 1。
- `scripts/run-refactor-gates.ps1`：通过。
