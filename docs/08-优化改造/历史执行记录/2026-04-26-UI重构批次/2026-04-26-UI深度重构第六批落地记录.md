# 2026-04-26 UI深度重构第六批落地记录

## 本批目标

1. 继续拆分 `AdminHomePage`，将首页级运营卡片与动态流沉淀为独立 section。
2. 收口 `ParentPaymentPage`、`TeacherAttendancePage` 的页面层视觉字面量。
3. 继续清理 `AdminSummaryHeroCard`、`TeacherSessionHeroCard`、`ParentStudentHeroCard` 的残余硬编码 token。

## 本批落地

### 1. AdminHomePage 首页 section 化继续推进

- `AdminHomePage` 已切换为 `DashboardPageTemplate` 编排。
- 首页工作台入口与运营动态继续下沉到独立区块组件：
  - `entry/src/main/ets/components/common/AdminHomeSections.ets`
- 页面内仅保留数据加载、路由跳转与 section 组装逻辑：
  - `entry/src/main/ets/pages/admin/AdminHomePage.ets`

### 2. ParentPaymentPage 页面层 token 收口

- 页面内的 `Color.White`、支付金额危险色、支付说明浅底色、底部栏阴影等视觉字面量统一改为 `HarmonyDesign` token：
  - `HarmonyColor.surface`
  - `HarmonyColor.textInverse`
  - `HarmonyReport.danger`
  - `HarmonyReport.cautionSurface`
  - `harmonyShadowColor()`
- 目标文件：
  - `entry/src/main/ets/pages/parent/ParentPaymentPage.ets`

### 3. TeacherAttendancePage 页面层 token 收口

- 统计卡片和内联指标色统一映射到 `HarmonyReport`：
  - 今日班次 -> `HarmonyReport.accent`
  - 已签到 -> `HarmonyReport.positive`
  - 未签到 -> `HarmonyReport.caution`
- 卡片底色、面板浅底、阴影统一改为：
  - `HarmonyColor.surface`
  - `HarmonyColor.surfaceSubtle`
  - `harmonyShadowColor()`
- 目标文件：
  - `entry/src/main/ets/pages/teacher/TeacherAttendancePage.ets`

### 4. Hero 组件残余 token 与文案清理

- 统一清理 Hero 组件中的残余硬编码与旧乱码文案：
  - `entry/src/main/ets/components/common/AdminSummaryHeroCard.ets`
  - `entry/src/main/ets/components/common/TeacherSessionHeroCard.ets`
  - `entry/src/main/ets/components/common/ParentStudentHeroCard.ets`
- 处理内容：
  - 默认金额文案改为明确值 `￥0`
  - 残留乱码标签恢复为正常中文
  - `build()` / `@Builder` 中不再使用局部 `const`，避免 ArkUI 编译器将其识别为非法根节点结构

## 验证结果

已执行：

- `hvigorw.bat assembleHap --analyze=advanced`
- `powershell -ExecutionPolicy Bypass -File scripts/check-page-budget.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/check-ui-governance.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/run-refactor-gates.ps1`

结果：

- 构建通过
- 页面体量门禁通过
- UI 治理门禁通过
- 重构总闸门通过

## 当前指标

- 页面层硬编码视觉 token：`101`
- `components/common` 硬编码视觉 token：`116`
- 超过 450 行页面：`0`
- `AdminHomePage` 当前行数：`342`

## 下一批优先项

1. 继续处理 `AdminSessionsPage`，优先压缩页面层字面量与区块体量。
2. 收口 `TeacherStudentDetailPage` 的页面层视觉 token。
3. 继续清理 `ParentOrderCreateSections`、`AdminSessionSections`、`TeacherPerformanceCard`、`MessageListItem`、`ProfileCenterSections` 的通用组件残余字面量。
