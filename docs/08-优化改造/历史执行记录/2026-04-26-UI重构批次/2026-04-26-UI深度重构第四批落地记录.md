# SmartGuardian UI 深度重构第四批落地记录
日期：2026-04-26

## 本轮目标

- 继续压缩首页与家长场景页的残余硬编码视觉 token
- 清理 `Card`、`PageHeader` 两个通用底层组件中的视觉字面量
- 将 `ParentSessionsPage`、`ParentQRCodePage`、`ParentAlertsPage` 收进统一工作台模板

## 本轮落地

### 1. 家长场景页切入统一工作台模板

- 页面：
  - `entry/src/main/ets/pages/parent/ParentSessionsPage.ets`
  - `entry/src/main/ets/pages/parent/ParentQRCodePage.ets`
  - `entry/src/main/ets/pages/parent/ParentAlertsPage.ets`
- 本轮收口内容：
  - 三页统一切到 `WorkbenchListPageTemplate`
  - 统一使用 `PageHeader + StatefulContainer + StatCardRow`
  - 首次加载、空态、错误、重试动作回收到同一套交互协议
  - 原先页面层自绘头部、手写 loading/empty 和散装卡片样式已移除

### 2. 家长预警页继续拆段

- 新增预警区块组件：
  - `entry/src/main/ets/components/common/ParentAlertSections.ets`
- 新增内容：
  - `ParentAlertResponseCard`
  - `ParentAlertListItemCard`
  - `ParentAlertDetailSheetCard`
- 结果：
  - `ParentAlertsPage` 从页面内大段卡片/详情渲染改为区块装配
  - 页面体量已重新压回预算线以内

### 3. 底层组件令牌化

- 组件：
  - `entry/src/main/ets/components/common/Card.ets`
  - `entry/src/main/ets/components/common/PageHeader.ets`
  - `entry/src/main/ets/components/common/ParentScheduleCard.ets`
- 本轮收口内容：
  - `Card` 的状态卡、动作卡默认色改用 `HarmonyColor / HarmonyReport`
  - `PageHeader` 重写为统一令牌化实现，白色透明层、反色文本和渐变背景不再散落写死
  - `ParentScheduleCard` 的提示背景改为设计令牌

### 4. 首页残余 token 压缩

- 页面：
  - `entry/src/main/ets/pages/parent/ParentHomePage.ets`
- 本轮收口内容：
  - 功能入口背景色、图标色、签到/作业/签退状态色统一改走 `HarmonyColor / HarmonyReport / RolePalette`
  - 家长首页已退出 UI 治理高噪声榜单

## 验证结果

已执行：

- `cmd /c hvigorw.bat assembleHap --analyze=advanced`
- `powershell -ExecutionPolicy Bypass -File scripts/check-page-budget.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/check-ui-governance.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/run-refactor-gates.ps1`

结果：

- `hvigor` 构建通过
- 页面体量门禁通过
- UI 治理门禁通过
- 全量重构门禁通过

## 指标变化

- 页面层硬编码视觉 token：`203`
- `components/common` 硬编码视觉 token：`146`
- 页面体量超 450 行：`0`

## 当前剩余重点

- 页面层高噪声文件已转移到：
  - `TeacherHomePage.ets`
  - `ParentServiceDetailPage.ets`
  - `AdminAlertsPage.ets`
  - `ParentOrderDetailPage.ets`
  - `AdminRefundsPage.ets`
- 通用组件层高噪声文件已转移到：
  - `AdminSummaryHeroCard.ets`
  - `TeacherSessionHeroCard.ets`
  - `ParentStudentHeroCard.ets`
  - `ParentOrderCreateSections.ets`
  - `MessageListItem.ets`

## 下一批建议

- 继续处理 `TeacherHomePage` 的首页分段与响应式拆分
- 清理 `ParentServiceDetailPage`、`ParentOrderDetailPage` 的页面层视觉字面量
- 将 `AdminAlertsPage` 与 `AdminRefundsPage` 迁入同一套工作台模板与状态协议
