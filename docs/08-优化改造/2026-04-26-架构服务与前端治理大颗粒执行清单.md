# 2026-04-26 架构服务与前端治理大颗粒执行清单

> 范围：`entry/` ArkTS 前端、`cloud-functions/` AGC Serverless、`docs/08-优化改造/` 治理文档  
> 基线：以 HarmonyOS ArkUI / UI 设计、AGC Cloud DB / Cloud Functions 官方文档为准  
> 目标：继续清理当前仓库里仍然存在的兼容壳层、重复契约、残余高噪声页面与文档目录漂移

## 参考基线

- HarmonyOS UI 设计简介  
  https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/ui-design-introduction
- ArkUI 指南  
  https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkui
- Cloud DB 新增对象类型  
  https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/cloudfoundation-database-add-object
- Cloud DB 部署数据库  
  https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/agc-harmonyos-clouddev-deploydatabase

## 当前判断

- 前端主骨架、状态协议、页面模板和 AGC Serverless 主链路已经成型，但仓库里还残留一批“迁移过渡层”和“治理末梢噪声”。
- 服务配置层目前最明显的问题不是功能缺口，而是兼容字段、重复契约和未收口的环境说明仍然存在。
- UI 层的大体量页面已经清掉，但页面层和 `components/common` 还各剩 15 个硬编码视觉 token，说明底层组件和少数页面仍有尾项。
- 文档目录已经形成主线，但还缺一份面向“退役/删除/归档/整理”的执行总表。

## 执行顺序

### P0 先做：立即退役与删除

- [x] 退役 `backend/` 旧 Spring Boot 包，移除前端 `TEST_REAL` 与登录页 Legacy Backend 入口，`docs/06-部署与运维` 改为 AGC Serverless 运维口径。

- [ ] 退役前端请求层无引用兼容壳层  
  文件：`entry/src/main/ets/config/api.config.ts`  
  动作：删除 `USE_MOCK_DATA`、`BASE_URL`、`MockSwitch` 这类只为旧实现保留但当前无引用的静态兼容字段。

- [ ] 删除重复的 AGC 服务契约目录  
  文件：`entry/src/main/ets/services/api/AgcServiceContracts.ts`  
  动作：保留 `services/agc/AgcFunctionContracts.ts` 作为唯一真实路由契约，删除未被引用的重复目录，并同步清理 `services/api/index.ts` 导出。

- [ ] 收紧示例配置说明  
  文件：`entry/src/main/ets/config/api.config.example.ts`  
  动作：把说明改成与当前运行时状态机一致，只保留仍然成立的环境切换说明。

### P1 继续做：服务配置整理与请求层收口

- [ ] 梳理 `ApiConfig` 的运行时状态机  
  文件：`entry/src/main/ets/config/api.config.ts`、`entry/src/main/ets/pages/LoginPage.ets`  
  动作：继续把展示名称、环境切换和运行时迁移逻辑压到单一入口，避免页面层再感知遗留兼容语义。

- [ ] 规范 AGC 请求适配层  
  文件：`entry/src/main/ets/services/agc/AgcRequestAdapter.ts`  
  动作：去掉无实际调用的占位 API，保留当前真实可用的 HTTP Gateway 路径；把未完成的 SDK 直调分支标成明确的待办或删除。

- [ ] 统一 API 导出边界  
  文件：`entry/src/main/ets/services/api/index.ts`  
  动作：只导出真实业务服务，不再让目录同时承担契约说明和 API 入口两种职责。

### P2 接着做：UI 治理尾项

- [ ] 清理剩余页面层硬编码视觉 token  
  目标页：`TeacherSchedulePage`、`ParentServicesPage`、`ParentChildrenPage`、`ParentAboutPage`、`ParentProfileEditPage`

- [ ] 清理剩余通用组件层硬编码视觉 token  
  目标组件：`WorkbenchActionGrid`、`StudentProfileSections`、`AdminOrderSections`、`ActionButton`、`ReportControlDeck`

- [ ] 继续把角色页切到统一模板  
  优先场景：家长资料编辑、服务列表、孩子列表、关于页、教师日程页

### P3 最后做：文档整理、归档与目录治理

- [ ] 给 `docs/08-优化改造/README.md` 补当前主线索引
- [ ] 归档已经被 2026-04-25/26 新主线文档覆盖的旧说明
- [ ] 保留“方案文档 + 收口记录 + 目录索引”三层结构，不再继续散落同类说明

## 本轮落地目标

- 完成 P0 全部项
- 开始 P2 第一批 UI 尾项收口
- 跑通 `check-agc-consistency`、`check-ui-governance`、`assembleHap`
