# 学生智慧托管系统数据库设计说明书

> 项目名称：基于鸿蒙系统的学生智慧托管系统  
> 文档类型：数据库设计说明书  
> 文档编号：SG-DB-001  
> 版本：V1.2  
> 状态：正式交付版  
> 日期：2026-04-16  
> 页眉建议：基于鸿蒙系统的学生智慧托管系统｜数据库设计说明书  
> 页脚建议：SmartGuardian 正式交付版｜第 X 页 / 共 Y 页

---

## 目录

- [1. 设计目标](#1-设计目标)
- [2. 数据库选型](#2-数据库选型)
- [3. 设计原则](#3-设计原则)
- [4. 核心表说明](#4-核心表说明)
- [5. 关键关系](#5-关键关系)
- [6. 索引与约束设计](#6-索引与约束设计)
- [7. 审计与逻辑删除](#7-审计与逻辑删除)
- [8. 性能建议](#8-性能建议)
- [9. 安全建议](#9-安全建议)

---

## 1. 设计目标

数据库设计用于支撑用户认证、学生档案、托管服务、订单审核、排课、考勤、作业反馈、消息互动、时间线与统计分析等核心场景，并与现有 SQL 建表脚本保持一致。

## 2. 数据库选型

- 数据库：MySQL 8.0+
- 字符集：utf8mb4
- 排序规则：utf8mb4_unicode_ci
- 存储引擎：InnoDB

## 3. 设计原则

1. 满足业务实体完整建模。  
2. 兼顾数据一致性与高频查询性能。  
3. 对敏感字段预留脱敏、加密与审计能力。  
4. 统一逻辑删除和审计字段。  
5. 支撑高峰签到与报表统计扩展需求。

## 4. 核心表说明

### 4.1 用户与学生域
- `user`：存储家长、教师、管理员账号信息。
- `student`：存储学生基本档案信息。
- `guardian_relation`：存储学生与监护人绑定关系。

### 4.2 托管服务域
- `service_product`：托管服务产品定义。
- `order_info`：家长预约下单与审核数据。
- `session_schedule`：班次、排课与教师分配数据。

### 4.3 执行与互动域
- `attendance_record`：签到签退与异常记录。
- `homework_task`：作业任务数据。
- `homework_feedback`：辅导反馈与家长确认数据。
- `message_record`：消息会话记录。
- `student_timeline`：学生动态时间线。

## 5. 关键关系

- 用户与学生通过 `guardian_relation` 建立多对多关系。  
- 学生与订单、考勤、作业、时间线均为一对多关系。  
- 服务产品与订单、班次为一对多关系。  
- 作业任务与作业反馈为一对多关系。  
- 消息记录通过发送人与接收人与用户关联。

## 6. 索引与约束设计

增强版 SQL 中已包含：
- 用户账号、用户名、手机号唯一约束
- 学生编号唯一约束
- 订单号唯一约束
- 学生+日期+班次唯一签到约束
- 基于角色、状态、日期、班次、会话的复合索引

## 7. 审计与逻辑删除

核心表普遍包含：
- `is_deleted`
- `created_by`
- `updated_by`
- `created_at`
- `updated_at`

该设计便于保留历史数据、支持追溯与审计。

## 8. 性能建议

- 对考勤、消息、时间线等大表进行分区/归档。  
- 对高频查询条件建立覆盖索引。  
- 结合 Redis 缓存热点业务数据。  
- 统计任务异步化，避免阻塞主业务链路。

## 9. 安全建议

- 对手机号、证件号、健康信息等敏感字段采用列级加密。  
- 数据导出默认脱敏处理。  
- 数据库账户按职责进行最小权限隔离。  
- 审计日志与业务库分域管理。
