# SmartGuardian AGC 上线检查清单

> 更新日期：2026-04-26  
> 状态：旧 `backend/` Spring Boot 包已退役。

## 1. AGC 后端

- [ ] Cloud DB Zone 名称与环境变量一致。
- [ ] Cloud DB 对象类型已导入，集合、索引、权限与本地 schema 对齐。
- [ ] `node scripts\apply-cloud-db-seed.js --reset` 在目标环境按需执行并通过。
- [ ] Cloud Functions 已按 manifest 部署。
- [ ] API Gateway 地址可访问，并已配置到前端运行环境。

## 2. 前端应用

- [ ] `entry/src/main/resources/rawfile/agconnect-services.json` 使用真实 AGC 客户端配置。
- [ ] 登录页只保留 `Mock` 与 `AGC` 两种环境入口。
- [ ] `AGC_SERVERLESS` 模式下登录、刷新会话、工作台 manifest、核心业务列表可用。
- [ ] Push/通知点击回调、LocationKit 签到位置链路按目标设备验证。

## 3. 构建与签名

- [ ] `hvigorw.bat assembleHap` 通过。
- [ ] `scripts\run-refactor-gates.ps1` 通过。
- [ ] DevEco Signing Configs 已生成匹配当前材料的加密签名配置。
- [ ] `agconnect-services.json`、API Client 凭证、签名材料未提交到版本控制。

## 4. 退役确认

- [ ] 不再启动 Spring Boot。
- [ ] 不再使用 MySQL 本地容器。
- [ ] 不再配置 `http://127.0.0.1:8080`。
- [ ] `backend/` 删除后，构建和治理门禁仍通过。
